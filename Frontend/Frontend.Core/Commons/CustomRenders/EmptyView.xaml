<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Frontend.Core.Commons.CustomRenders.EmptyView">
    <ContentView.Content>
        <StackLayout
            VerticalOptions="CenterAndExpand"
            HorizontalOptions="CenterAndExpand">
            <Image x:Name="imageEmpty" 
                   Margin="0 , 0 , 0 , 15"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label x:Name="titleText" Text="No hay datos"
                   FontSize="Medium"
                   FontAttributes="Bold"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label x:Name="detailText" Text="No hay datos disponibles"
                   FontSize="Micro"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Button x:Name="actionButton" Text="Volver a sincronizar"
                    BackgroundColor="Transparent"
                    FontAttributes="None"
                    Margin="0, 10, 0, 0"
                    TextColor="#1b60ab"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentView.Content>
</ContentView>